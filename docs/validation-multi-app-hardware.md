# 多应用与多 Wi-Fi 实机验证

设备为已核对的 ESP32-S3 PhotoPainter N16R8。旧固件升级前完整备份 16 MiB Flash，并用设备端摘要核对一致；保留备份在仓库外。原定时推图在测试期间停止，结束时恢复。

## 已验证

- 宿主源码 `9602f4d` 首次部署成功，原网络 NVS 与 photoframe A/B 导入成功，版本保持 15/14。
- 独立应用源码位于 photoframe `8c2de88` 的 `examples/color-test`，有自己的工程与入口。v1 为正序色带，v2 为逆序色带；两份 ELF 的 SHA 不同。共用解码和驱动源码不构成宿主构建依赖。
- color-test 安装、激活成功；错误 PNG 返回 422 且不确认。省略应用 ID 的旧推图请求返回 409，没有误投给 color-test。
- 首次安装的候选在未确认时复位，恢复 photoframe。原两个槽总计 2 MiB 经设备摘要比对，与备份逐字节相同。
- color-test v1 首次刷新、v2 更新刷新、回退 v1 刷新，以及切回 photoframe 的最终刷新均返回 HTTP 200。
- 两个已确认应用之间的切换在未确认时复位，恢复原来选择的 color-test，双方 A/B 不变。
- 临时 NVS 配置两组网络：随机不可达 SSID 在前，现有网络在后。脱敏串口记录显示尝试 1/2 → 2/2；首轮失败后重新遍历 1/2 → 2/2，最终取得 IP。不是两个真实 AP 的切换测试。随后恢复备份中的原网络 NVS。
- 结束状态：当前 photoframe，版本 15/14；已安装 color-test，版本 1/2；两者 active=A、previous=B，无 pending/trial。定时服务已恢复。

首轮部署的宿主 ELF SHA-256：`68d55a058e17a884bf584766e889067138b6f5bd83e665f8847f2933c4709f7b`。

color-test ELF SHA-256：

- v1：`ae1f1243df18deda5e46efe8fb7f645595a62399ebb0e445d20516b7d1958c42`
- v2：`30dc9fc96b35031b50217fdf0068c553b9a88a5c778d4fcd87d15278d6686ff5`

## 补齐首次 SD 迁移后的部署

宿主 `a554ebe` 已在完整备份当前两个应用状态后部署；只写宿主分区。启动日志确认版本 `a554ebe`、ELF 摘要前缀 `0cec5ac6a`、旧 NVS 单组配置（1/1）取得 IP。管理接口再次返回 200，原 photoframe 15/14 与 color-test 1/2 保持不变。

- 宿主 ELF SHA-256：`0cec5ac6a4bcdba0ad785aa87fdab09e690136bb1d35916718c8337c056fb365`
- 宿主固件 SHA-256：`d686e37e96ef5e08a2d0783040588b57d93e167436ebf182eab65d618182913c`
- 本地验证：宿主 61 项测试全部通过，color-test 3 项测试全部通过；实际 SD 卡写入仍未验收。

最终已恢复 LaunchAgent，并从恢复后的新日志确认 `2026-09-06T13:48:02.072+08:00` 自动推图成功。该成功消息只在客户端取得 HTTP 200 后写出。

串口关闭后的首个状态请求曾被连接重置；同一设备的只读复查立即返回正常状态，未据此再次刷写。

## 尚未证明

- 实体 SD 卡的读写、首次迁移与拔卡重启；当前只有代码、文件系统单测和构建证据。
- 两个真实 2.4 GHz AP 的可控开关及网络丢失恢复。当前实机测试是一组不可达配置加一组真实网络。
- 刷写中途的真实断电。已测试真实复位取消候选，以及 native Flash/NVS 写入失败注入；二者不等于真实断电测试。
- 本次色带和最终额度页面的人工视觉确认尚待用户反馈；HTTP 200 证明固件执行完成，不代替视觉检查。

初始串口采集曾未取得应用日志；显式将 DTR/RTS 设为 false 后用 esptool 的 USB reset 实现取得正常启动与尝试序列。没有据此重刷网络固件，也没有将采集失败误报成 Wi-Fi 失败。

精确 HTTP 结果、状态快照与脱敏尝试时间保存在本地证据文件；凭据、原始 NVS 和 Flash 备份不入库。
